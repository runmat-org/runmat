% Quadratic curve test
x = [-2, -1.5, -1, -0.5, 0, 0.5, 1, 1.5, 2];
y = [4, 2.25, 1, 0.25, 0, 0.25, 1, 2.25, 4];  % x^2 values

% Create the plot
plot(x, y);

% Calculations
vertex_x = 0;
vertex_y = 0;
discriminant = 0;