//! Integration tests for `runmat-runtime`.
//!
//! This crate exists solely to host the GPU-centric test suites that depend on
//! `runmat-accelerate`, so that the `runmat-runtime` crate can publish without a
//! feature-edge dependency on `runmat-accelerate`.

#![allow(dead_code)]
