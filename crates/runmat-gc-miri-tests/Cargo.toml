[package]
name = "runmat-gc-miri-tests"
version = "0.5.4"
edition = "2021"
publish = false
description = "Miri-focused soundness tests for the RunMat garbage collector"
license-file = "../../LICENSE.md"

[dependencies]
runmat-builtins = { workspace = true }
runmat-gc = { workspace = true }
