{
  "title": "primes",
  "category": "math/discrete",
  "keywords": [
    "primes",
    "prime",
    "number theory",
    "discrete math",
    "sieve"
  ],
  "summary": "Return prime numbers less than or equal to a scalar bound in MATLAB and RunMat.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [],
    "broadcasting": "none",
    "notes": "gpuArray scalar inputs are gathered to the host. The output is a host row vector because primes is a variable-size generator rather than an element-wise GPU operation."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 1,
    "constants": "none"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::math::discrete::primes::tests",
    "integration": null
  },
  "description": "`primes(n)` returns a 1-by-N row vector containing every prime number less than or equal to scalar real integer `n`. Values below 2 return an empty 1-by-0 row vector.",
  "behaviors": [
    "`n` must be a scalar, finite, real integer value.",
    "Double scalar inputs return a double row vector.",
    "Single and supported unsigned integer tensor/scalar inputs preserve their output dtype (`single`, `uint8`, `uint16`, or `uint32`).",
    "Signed integer and `uint64` scalar bounds are accepted and return a double row vector until RunMat tensors include signed integer and `uint64` array storage.",
    "Logical, complex, non-scalar, non-finite, and fractional inputs raise an error.",
    "Requests above RunMat's bounded sieve safety limit are rejected before allocation."
  ],
  "examples": [
    {
      "description": "Prime numbers less than or equal to 25",
      "input": "p = primes(25)",
      "output": "p = [2 3 5 7 11 13 17 19 23]"
    },
    {
      "description": "Prime numbers less than or equal to an unsigned integer",
      "input": "p = primes(uint16(12))",
      "output": "p = uint16([2 3 5 7 11])"
    },
    {
      "description": "No prime numbers below two",
      "input": "p = primes(1)",
      "output": "p = 1x0 empty double row vector"
    }
  ],
  "faqs": [
    {
      "question": "Does primes accept arrays?",
      "answer": "No. MATLAB defines `n` as a scalar upper bound, and RunMat rejects non-scalar inputs."
    },
    {
      "question": "Why does RunMat reject very large values of n?",
      "answer": "The implementation uses a host sieve and rejects requests above its safety limit before allocation to avoid unbounded memory use."
    },
    {
      "question": "Does primes keep integer output classes?",
      "answer": "RunMat preserves supported unsigned integer tensor classes. Signed integer and `uint64` array classes are not yet represented by RunMat tensors, so those scalar bounds are accepted and returned as double row vectors for compatibility with common MATLAB call sites."
    }
  ],
  "links": [
    {
      "label": "factorial",
      "url": "./factorial"
    },
    {
      "label": "nchoosek",
      "url": "./nchoosek"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/math/discrete/primes.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/math/discrete/primes.rs"
  }
}
