{
  "title": "erasePunctuation",
  "category": "strings/transform",
  "keywords": [
    "erasePunctuation",
    "punctuation",
    "symbols",
    "text analytics",
    "string"
  ],
  "summary": "Remove Unicode punctuation and symbol characters from text and tokenized documents.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [],
    "broadcasting": "none",
    "notes": "Text values are host-resident. GPU-resident wrapper inputs are gathered before evaluating this string transform."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 3,
    "constants": "inline"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::strings::transform::compat::tests::splitlines_reverse_deblank_and_matches_work",
    "integration": null
  },
  "description": "`erasePunctuation(text)` removes characters in Unicode punctuation and symbol categories from string, character, and cell text inputs while preserving the input container shape and type. `erasePunctuation(documents)` filters punctuation and symbol tokens from RunMat `tokenizedDocument` compatibility objects.",
  "behaviors": [
    "String scalars and string arrays return string values with punctuation and symbol characters removed from each nonmissing element.",
    "Character vectors and character arrays return character arrays; rows are padded after punctuation removal when needed to preserve rectangular char storage.",
    "Cell arrays are processed recursively for string and character text elements and retain the original cell shape.",
    "Missing string elements are preserved.",
    "`erasePunctuation(documents)` removes punctuation and symbol characters from tokens with derived type `punctuation` or `other` and removes tokens that become empty.",
    "`erasePunctuation(documents,'TokenTypes',types)` removes punctuation and symbols only from the specified derived token types. RunMat derives token types from the lightweight stored tokens rather than a full MATLAB `tokenDetails` table.",
    "For tokenized documents, URLs, email addresses, hashtags, mentions, and emoticons are preserved by default."
  ],
  "examples": [
    {
      "description": "Erase Punctuation From A String",
      "input": "erasePunctuation(\"it's one and/or two.\")",
      "output": "\"its one andor two\""
    },
    {
      "description": "Erase Punctuation And Symbols From A Character Vector",
      "input": "erasePunctuation('cost: $5.00!')",
      "output": "'cost 500'"
    },
    {
      "description": "Preserve Cell Array Shape",
      "input": "erasePunctuation({'alpha,beta!','C++ and C#'})",
      "output": "{'alphabeta','C and C'}"
    },
    {
      "description": "Erase Punctuation From Tokenized Documents",
      "input": "documents = tokenizedDocument(\"An example: visit https://example.com.\");\nnewDocuments = erasePunctuation(documents)",
      "output": "`newDocuments` contains `An example visit https://example.com`."
    }
  ],
  "faqs": [
    {
      "question": "Does `erasePunctuation` remove spaces?",
      "answer": "No. Whitespace is not in a Unicode punctuation or symbol category, so spaces are preserved."
    },
    {
      "question": "Does this implement tokenizedDocument token-type filtering?",
      "answer": "Yes for RunMat's lightweight `tokenizedDocument` objects. It supports default `punctuation`/`other` filtering and explicit `TokenTypes`; full custom token-detail tables remain tracked separately."
    }
  ],
  "links": [
    {
      "label": "eraseURLs",
      "url": "./eraseURLs"
    },
    {
      "label": "string",
      "url": "./string"
    },
    {
      "label": "char",
      "url": "./char"
    }
  ],
  "syntax": {
    "example": {
      "description": "Supported forms",
      "input": "newText = erasePunctuation(text)\nnewDocuments = erasePunctuation(documents)\nnewDocuments = erasePunctuation(documents,'TokenTypes',types)",
      "output": "`newText` has the same text container shape and type as `text`. `newDocuments` is a tokenizedDocument compatibility object."
    },
    "points": [
      "`text` may be a string scalar, string array, character vector, character array, or cell array containing text values.",
      "`documents` must be a RunMat `tokenizedDocument` object.",
      "`types` may be a string scalar/vector, character vector, or cell array of character vectors naming derived token types.",
      "Punctuation and symbol removal follows Unicode punctuation (`P*`) and symbol (`S*`) categories."
    ]
  },
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/strings/transform/compat.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/strings/transform/compat.rs"
  },
  "gpu_residency": "`erasePunctuation` operates on host text and has no provider kernel."
}
