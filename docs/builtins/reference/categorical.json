{
  "title": "categorical",
  "category": "table",
  "keywords": ["categorical", "categories", "table", "ordinal"],
  "summary": "Create a categorical array object.",
  "gpu_support": {"elementwise": false, "reduction": false, "precisions": [], "broadcasting": "none", "notes": "Categorical arrays are host metadata objects. Current public documentation lists GPU code generation rather than interactive gpuArray input; RunMat can gather resident arguments only when RunMat extensions are enabled."},
  "fusion": {"elementwise": false, "reduction": false, "max_inputs": 2, "constants": "inline"},
  "requires_feature": null,
  "tested": {"unit": "builtins::table::tests", "integration": "crates/runmat-vm/tests/tables_timetables.rs"},
  "description": "`categorical(A)` maps input labels to integer category codes and stores category labels in object metadata.",
  "behaviors": ["String, char, cellstr, numeric, and logical inputs are supported; numeric input includes all eight integer classes.", "`categorical(A)` derives sorted unique categories from non-missing labels, and numeric default names are rounded to five significant figures; values too close to retain unique names reject.", "Leading and trailing spaces are removed from string and character-vector input values and from textual `valueset` entries.", "`categorical(A, valueset)` requires `valueset` to have the same class as `A`, uses the supplied values as the category order, and marks unmatched labels as missing.", "`categorical(A, valueset, catnames)` maps source values through unique `valueset` entries and stores the corresponding category names from `catnames`; repeated category names deliberately merge distinct source values.", "Missing source values become undefined unless a corresponding missing `valueset` entry and nonmissing category name explicitly maps them.", "`\"Ordinal\"` and `\"Protected\"` accept logical or numeric scalar zero or one, including all eight integer classes; ordinal categoricals are always protected. Those option words remain positional labels when not followed by a valid flag.", "Categorical-to-categorical comparisons require matching category lists.", "Ordinal categorical arrays support equality, relational comparisons, `min`, and `max` by category order.", "Use `ordinal(...)` as a compatibility constructor for categoricals whose category order is meaningful."],
  "examples": [{"description": "Create categories", "input": "C = categorical({'red'; 'blue'; 'red'})", "output": "C = categorical object"}],
  "links": [{"label": "iscategorical", "url": "./iscategorical"}, {"label": "ordinal", "url": "./ordinal"}, {"label": "isordinal", "url": "./isordinal"}],
  "source": {"label": "`crates/runmat-runtime/src/builtins/table/mod.rs`", "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/table/mod.rs"}
}
