{
  "title": "addTypeDetails",
  "category": "strings/text_analytics",
  "keywords": [
    "addTypeDetails",
    "tokenizedDocument",
    "text analytics",
    "token types"
  ],
  "summary": "Add token type details to tokenized documents.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [],
    "broadcasting": "none",
    "notes": "`addTypeDetails` classifies host-resident text tokens and updates tokenizedDocument metadata; it does not use provider kernels."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 3,
    "constants": "inline"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::strings::text_analytics::details::tests",
    "integration": null
  },
  "description": "`addTypeDetails(documents)` detects token types in a RunMat `tokenizedDocument` compatibility object and returns an updated object. `addTypeDetails(documents,'DiscardKnownValues',tf)` controls whether existing type details are preserved or recomputed.",
  "behaviors": [
    "`documents` must be a RunMat `tokenizedDocument` object.",
    "When type details are absent, RunMat derives one type value for each token and stores the details on the returned object.",
    "`DiscardKnownValues` defaults to `false`, so existing type details are preserved.",
    "`DiscardKnownValues`, `true` recomputes every stored type detail.",
    "RunMat recognizes `letters`, `digits`, `punctuation`, `email-address`, `web-address`, `hashtag`, `at-mention`, `emoticon`, `emoji`, and `other` using the same lightweight token rules as `tokenizedDocument`.",
    "The MATLAB `TopLevelDomains` option, custom token-type tables, and exact MathWorks URL classifier parity remain broader Text Analytics work."
  ],
  "examples": [
    {
      "description": "Add Types To Pre-Tokenized Documents",
      "input": "documents = tokenizedDocument([\"For\" \"more\" \"information\" \",\"], \"TokenizeMethod\", \"none\");\ndocuments = addTypeDetails(documents);\ntdetails = tokenDetails(documents)",
      "output": "`tdetails.Type` contains `letters`, `letters`, `letters`, and `punctuation`."
    },
    {
      "description": "Recompute Type Details",
      "input": "documents = addTypeDetails(documents, \"DiscardKnownValues\", true)",
      "output": "`documents` has refreshed token type details."
    }
  ],
  "faqs": [
    {
      "question": "Does `addTypeDetails` support `TopLevelDomains`?",
      "answer": "Not in this slice. RunMat uses the current lightweight URL detector and keeps full top-level-domain customization in the broader Text Analytics queue."
    },
    {
      "question": "Does `addTypeDetails` execute on the GPU?",
      "answer": "No. It classifies host token strings and updates object metadata."
    }
  ],
  "links": [
    {
      "label": "tokenDetails",
      "url": "./tokenDetails"
    },
    {
      "label": "tokenizedDocument",
      "url": "./tokenizedDocument"
    }
  ],
  "syntax": {
    "example": {
      "description": "Supported forms",
      "input": "updatedDocuments = addTypeDetails(documents)\nupdatedDocuments = addTypeDetails(documents, 'DiscardKnownValues', tf)",
      "output": "`updatedDocuments` is a `tokenizedDocument` compatibility object."
    },
    "points": [
      "`documents` must be a RunMat `tokenizedDocument` object.",
      "`tf` must be a logical scalar or numeric 0/1 scalar.",
      "Use `tokenDetails(updatedDocuments)` to view the resulting type table."
    ]
  },
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/strings/text_analytics/details.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/strings/text_analytics/details.rs"
  },
  "gpu_residency": "`addTypeDetails` updates host tokenizedDocument metadata and has no provider kernel."
}
