{
  "title": "xlim",
  "category": "plotting",
  "keywords": ["xlim", "axis limits", "plotting"],
  "summary": "Query or set x-axis limits.",
  "description": "`xlim([xmin xmax])` sets the x-axis limits, while `xlim()` returns the current limits as a double vector.",
  "behaviors": ["The two-element limits vector may use single, double, or any native integer class.", "Native integer endpoints are ordered using their exact values before conversion to graphics coordinates.", "Endpoints must be finite, increasing, and remain distinct in the graphics coordinate domain.", "The supported text modes are `'auto'`, `'tight'`, and `'manual'`."],
  "examples": [{"description": "Set integer limits", "input": "plot(1:10);\nxlim(int16([0 12]))"}, {"description": "Query current limits", "input": "limits = xlim()"}],
  "links": [{"label": "ylim", "url": "./ylim"}, {"label": "axis", "url": "./axis"}],
  "source": {"label": "`crates/runmat-runtime/src/builtins/plotting/ops/xlim.rs`", "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/plotting/ops/xlim.rs"}
}
