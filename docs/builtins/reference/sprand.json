{
  "title": "sprand",
  "category": "array/creation",
  "keywords": [
    "sprand",
    "sparse",
    "random",
    "density"
  ],
  "summary": "Create sparse random matrices.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [
      "f64"
    ],
    "broadcasting": "none",
    "notes": "`sprand` uses RunMat's host RNG and returns host-resident sparse CSC storage."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 5,
    "constants": "inline"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::array::creation::sparse::tests::sprand_pattern_form_preserves_shape_and_pattern; builtins::array::creation::sparse::tests::sprand_density_form_constructs_requested_sparse_shape; builtins::array::creation::sparse::tests::sprand_scalar_rc_form_matches_requested_condition_at_full_density; builtins::array::creation::sparse::tests::sprand_vector_rc_form_uses_requested_singular_values; builtins::array::creation::sparse::tests::sprand_rc_form_respects_density_and_typename_validation; builtins::array::creation::sparse::tests::sprand_rc_vector_shape_preserves_singular_value_without_rotation_cap"
  },
  "description": "`sprand` creates sparse double matrices with random values and optional reciprocal-condition control.",
  "behaviors": [
    "`sprand(S)` preserves the nonzero pattern and shape of `S` while replacing stored values with uniform random values.",
    "`sprand(m, n, density)` creates an `m x n` sparse matrix with approximately `round(m*n*density)` stored entries.",
    "`density` must be finite and between `0` and `1`.",
    "`sprand(m, n, density, rc)` accepts a scalar reciprocal-condition target or a vector of singular values in `[0,1]`. RunMat starts from the requested singular profile and applies random orthogonal plane rotations to add sparse fill while preserving that profile.",
    "A trailing `\"double\"` or `\"single\"` typename selects the sparse output class.",
    "All eight integer classes are documented for the pattern input `S`. Typed integer dimensions, density, and condition controls are RunMat extensions; dimensions are structural, while numerical controls reject if binary64 conversion would round.",
    "Large requested sparse patterns, condition-profile dense intermediates, and excessive plane-rotation work are rejected before allocating or running unbounded work."
  ],
  "examples": [
    {
      "description": "Random values on an existing pattern",
      "input": "P = sparse([1; 3], [1; 2], [10; 20], 3, 2);\nR = sprand(P);\nissparse(R)",
      "output": "ans = 1"
    }
  ],
  "faqs": [
    {
      "question": "Does `sprand(...,rc)` enforce the reciprocal condition number?",
      "answer": "Yes. RunMat uses orthogonal row and column plane rotations, which preserve the singular values of the requested scalar reciprocal condition target or vector singular profile. The requested density remains approximate for small or structurally constrained shapes."
    }
  ],
  "links": [
    {
      "label": "sparse",
      "url": "./sparse"
    },
    {
      "label": "rand",
      "url": "./rand"
    },
    {
      "label": "sprandn",
      "url": "./sprandn"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/array/creation/sparse/mod.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/array/creation/sparse/mod.rs"
  },
  "syntax": {
    "example": {
      "description": "Supported forms",
      "input": "R = sprand(S)\nR = sprand(S, typename)\nR = sprand(m, n, density)\nR = sprand(m, n, density, typename)\nR = sprand(m, n, density, rc)\nR = sprand(m, n, density, rc, typename)",
      "output": "`R` is a sparse double matrix by default or sparse single when `typename` is `\"single\"`."
    },
    "points": [
      "The output is host-resident sparse CSC storage.",
      "`rc` can be a scalar reciprocal condition number or a vector of singular values with length at most `min(m,n)`.",
      "The `rc` form is bounded by dense working-storage and plane-rotation work limits.",
      "The output typename is `\"double\"` or `\"single\"`."
    ]
  },
  "gpu_residency": "`sprand` runs on the host RNG and produces a host sparse matrix."
}
