{
  "title": "dec2bin",
  "category": "comms/conversion",
  "keywords": [
    "dec2bin",
    "decimal",
    "binary",
    "base conversion",
    "twos complement"
  ],
  "summary": "Convert decimal integers to binary character rows in MATLAB and RunMat.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [],
    "broadcasting": "none",
    "notes": "gpuArray inputs are gathered to the host for integer validation and text formatting."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 1,
    "constants": "none"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::comms::binary_conversion::tests",
    "integration": null
  },
  "description": "`dec2bin(D)` converts each element of `D` to one binary character row. `dec2bin(D, minDigits)` pads each row to at least `minDigits` characters. Numeric arrays are processed in column-major element order and produce one output row per element.",
  "behaviors": [
    "`D` may be numeric, integer, logical, character, or gpuArray numeric data.",
    "Double inputs are floored before conversion, matching MATLAB `dec2bin` behavior.",
    "Nonnegative values are padded with `0` characters.",
    "Negative values are represented using the smallest signed two's-complement storage width that can contain the value, with additional padding using `1` characters.",
    "Signed integer inputs preserve their source integer storage width for negative two's-complement output.",
    "`minDigits` must be a finite nonnegative integer scalar.",
    "Outputs larger than RunMat's character-output safety limit are rejected before formatting.",
    "Complex, non-finite, and unsupported input values raise an error."
  ],
  "examples": [
    {
      "description": "Convert a scalar decimal value",
      "input": "s = dec2bin(23)",
      "output": "s = '10111'"
    },
    {
      "description": "Pad to a minimum width",
      "input": "s = dec2bin(23, 8)",
      "output": "s = '00010111'"
    },
    {
      "description": "Convert multiple values to padded rows",
      "input": "s = dec2bin([1023 122 14])",
      "output": "s = ['1111111111'; '0001111010'; '0000001110']"
    },
    {
      "description": "Format a negative value as two's complement",
      "input": "s = dec2bin(-16)",
      "output": "s = '11110000'"
    }
  ],
  "faqs": [
    {
      "question": "Does dec2bin round fractional inputs?",
      "answer": "No. RunMat floors fractional double inputs before conversion, following MATLAB `dec2bin` behavior."
    },
    {
      "question": "How are negative numbers represented?",
      "answer": "Negative values are emitted as two's-complement binary rows using 8, 16, 32, or 64 bits as needed."
    }
  ],
  "links": [
    {
      "label": "bin2dec",
      "url": "./bin2dec"
    },
    {
      "label": "dec2hex",
      "url": "./dec2hex"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/comms/binary_conversion.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/comms/binary_conversion.rs"
  }
}
