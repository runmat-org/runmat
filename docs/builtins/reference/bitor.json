{
  "title": "bitor",
  "category": "logical/bit",
  "keywords": ["bitor", "bitwise", "or", "integer"],
  "summary": "Compute bitwise OR for integer-valued scalars and arrays.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [],
    "broadcasting": "matlab",
    "notes": "Documented gpuArray input is uint8, uint16, or uint32 and uses an exact gather-and-restore fallback."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 2,
    "constants": "none"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::logical::bit::integer::tests",
    "integration": null
  },
  "description": "`bitor(A, B)` applies bitwise OR to same-class logical, integer, or finite nonnegative integer-valued double inputs using MATLAB-style implicit expansion. `bitor(A, B, assumedtype)` selects the signed or unsigned interpretation width for double inputs.",
  "behaviors": [
    "Accepts double integer-valued inputs, logical inputs, and all eight built-in integer classes.",
    "Supports implicit expansion across compatible shapes.",
    "Inputs must have the same data type unless one is a scalar double; the result has that shared type or the nondouble integer type.",
    "Logical inputs return logical output, integer inputs preserve their exact native class, and double inputs return double.",
    "Signed integer inputs and signed assumed types use two's-complement bit patterns.",
    "Rejects fractional, out-of-range, infinite, NaN, complex, mixed integer-class, string, cell, and struct inputs."
  ],
  "examples": [
    {
      "description": "Scalar bitwise OR",
      "input": "bitor(5, 3)",
      "output": "ans = 7"
    }
  ],
  "faqs": [
    {
      "question": "Can `bitor` broadcast scalar masks?",
      "answer": "Yes. Scalar masks expand across array inputs using MATLAB-compatible implicit expansion."
    }
  ],
  "links": [
    {
      "label": "bitand",
      "url": "./bitand"
    },
    {
      "label": "bitshift",
      "url": "./bitshift"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/logical/bit/integer.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/logical/bit/integer.rs"
  },
  "gpu_residency": "Documented uint8, uint16, and uint32 GPU inputs gather exactly for host execution and the result is restored to the owning provider.",
  "gpu_behavior": [
    "The documented GPU domain accepts two unsigned integer arrays, or one unsigned integer array and one scalar double; 64-bit integers and assumedtype are unsupported.",
    "RunMat mode separately gates single input, broader resident dtypes, and resident assumedtype calls as extensions."
  ]
}
