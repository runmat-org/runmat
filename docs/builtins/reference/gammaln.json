{
  "title": "gammaln",
  "category": "math/elementwise",
  "keywords": [
    "gammaln",
    "gamma",
    "log gamma",
    "special function",
    "elementwise",
    "gpu"
  ],
  "summary": "Compute the natural logarithm of the gamma function element-wise in MATLAB and RunMat.",
  "gpu_support": {
    "elementwise": true,
    "reduction": false,
    "precisions": [
      "f32",
      "f64"
    ],
    "broadcasting": "matlab",
    "notes": "Uses unary_gammaln for documented real single or double input after proving the domain. RunMat-only integer and logical extensions gather authoritatively, validate before provider access, and restore floating output to the owning provider."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 1,
    "constants": "inline"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::math::elementwise::gammaln::tests",
    "integration": "builtins::math::elementwise::gammaln::tests::gammaln_gpu_provider_roundtrip"
  },
  "description": "`Y = gammaln(A)` evaluates `log(gamma(A))` for real nonnegative inputs while avoiding the overflow that can occur when computing `gamma(A)` first.",
  "behaviors": [
    "`gammaln(A)` applies the log-gamma function element-by-element while preserving the input tensor shape.",
    "Double inputs return double results; single tensors preserve single dtype metadata with single-rounded output values.",
    "`gammaln(0)` returns `Inf`; `gammaln(Inf)` returns `Inf`; `gammaln(NaN)` returns `NaN`.",
    "Typed integer, logical, and character inputs are separate RunMat-only extensions and are rejected in strict compatibility mode.",
    "The integer extension admits all eight classes only when every value is exactly representable as binary64; output is double.",
    "Negative, complex, string, and sparse inputs produce stable typed errors.",
    "Real GPU tensors stay with their owning provider. Native output is validated for shape, precision, real storage, and ownership; all fallback results are restored to that owner."
  ],
  "examples": [
    {
      "description": "Evaluate the log-gamma function at a scalar",
      "input": "y = gammaln(5)",
      "output": "y = 3.1781"
    },
    {
      "description": "Evaluate a half-integer input",
      "input": "y = gammaln(0.5)",
      "output": "y = 0.5724"
    },
    {
      "description": "Apply gammaln element-wise to an array",
      "input": "A = [0.5 1 2 5];\nY = gammaln(A)",
      "output": "Y = [0.5724 0 0 3.1781]"
    },
    {
      "description": "Avoid gamma overflow for large inputs",
      "input": "y = gammaln(171)",
      "output": "y = 706.5731"
    },
    {
      "description": "Run gammaln on GPU data",
      "input": "G = gpuArray([0.5 1 5]);\nout = gammaln(G);\nresult = gather(out)",
      "output": "result = [0.5724 0 3.1781]"
    }
  ],
  "faqs": [
    {
      "question": "Why use `gammaln(A)` instead of `log(gamma(A))`?",
      "answer": "`gammaln` evaluates the logarithm directly, so large positive inputs can remain finite even when `gamma(A)` would overflow before the logarithm is taken."
    },
    {
      "question": "Does `gammaln` support complex or negative inputs?",
      "answer": "No. MATLAB documents `gammaln` for real nonnegative input, and RunMat reports typed errors for complex and negative values."
    },
    {
      "question": "Can `gammaln` run on GPU arrays?",
      "answer": "Yes for real nonnegative single or double tensors. A valid native result stays resident; otherwise RunMat gathers, computes, and uploads the precision-preserving result back to the input's owning provider."
    }
  ],
  "related": [
    {
      "label": "gamma",
      "url": "./gamma"
    },
    {
      "label": "log",
      "url": "./log"
    },
    {
      "label": "factorial",
      "url": "./factorial"
    },
    {
      "label": "gpuArray",
      "url": "./gpuarray"
    },
    {
      "label": "gather",
      "url": "./gather"
    }
  ],
  "source": [
    {
      "label": "`crates/runmat-runtime/src/builtins/math/elementwise/gammaln.rs`",
      "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/math/elementwise/gammaln.rs"
    }
  ],
  "gpu_residency": "Yes for admitted gpuArray inputs. Documented floating inputs use validated native output when possible; host fallback and RunMat-only integer or logical extension paths upload the result back to the original owning provider.",
  "implementation_notes": [
    "CPU evaluation uses a log-Lanczos approximation and the reflection identity for positive inputs below 0.5.",
    "The WGPU backend uses the same log-Lanczos form for f32 and f64 precision.",
    "RunMat validates the real nonnegative domain before executing provider kernels so negative inputs produce stable domain errors.",
    "Integer, logical, and character support consists of independently gated RunMat-only extensions. Integer admission uses the shared exact-binary64 predicate before conversion, including for resident storage."
  ]
}
