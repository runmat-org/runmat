{
  "title": "lasso",
  "category": "stats/ml",
  "keywords": [
    "lasso",
    "elastic net",
    "regularization",
    "regression",
    "statistics",
    "machine learning"
  ],
  "summary": "Fit lasso or elastic-net regularized linear regression models.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": ["f64"],
    "broadcasting": "none",
    "notes": "Automatic internal residency gathers transparently. Explicit gpuArray input is a mode-gated RunMat extension; the coordinate-descent solver runs on CPU."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 0,
    "constants": "none"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::stats::ml::lasso::tests",
    "integration": "crates/runmat-vm/tests/integer_json_join_and_statistical_semantics.rs"
  },
  "description": "`lasso(X,y)` fits regularized linear regression coefficients for predictor matrix `X` and response vector `y`. Columns of `B` correspond to ascending `Lambda` values, and `[B,FitInfo] = lasso(...)` returns MATLAB-compatible fit metadata.",
  "behaviors": [
    "`X` must be a finite real numeric matrix with observations in rows and predictors in columns. `y` must be a finite real vector whose length matches `size(X,1)`.",
    "`Lambda` accepts a nonnegative scalar or vector. RunMat sorts Lambda values in ascending order for output, matching MATLAB's `FitInfo.Lambda` convention.",
    "When `Lambda` is omitted, RunMat computes a geometric regularization path using `NumLambda` and `LambdaRatio` from the largest lambda that gives an all-zero model.",
    "`Alpha` supports lasso and elastic-net fits for values in `(0,1]`. `Alpha=1` is lasso; smaller positive values mix in the ridge penalty.",
    "`Standardize` and `Intercept` follow MATLAB's model form. If `Intercept` is false, RunMat disables standardization and returns zero intercepts.",
    "`Weights` accepts a nonnegative observation-weight vector with positive total weight. Weights are normalized internally.",
    "`RelTol` and `MaxIter` control coordinate-descent convergence. The `Iterations` FitInfo field records iterations per Lambda value.",
    "`CV` supports `\"resubstitution\"` and positive integer K-fold cross-validation. K-fold fits add `SE`, `LambdaMinMSE`, `Lambda1SE`, `IndexMinMSE`, and `Index1SE` fields to `FitInfo`.",
    "`PredictorNames`, `DFmax`, `UseCovariance`, and `MCReps=1` are accepted for script compatibility. Tall-array ADMM behavior, covariance-matrix acceleration, and the currently unimplemented `CacheSize`, `AbsTol`, `B0`, `U0`, `Rho`, and `Options` forms reject explicitly; `FitInfo.UseCovariance` is false because RunMat uses direct coordinate descent.",
    "Typed-integer X, y, floating parameters, and structural or logical controls are independently classified RunMat-only extensions. Integer data remains exact until a checked double solver boundary; values not exactly representable as double reject rather than round."
  ],
  "examples": [
    {
      "description": "Fit a sparse linear model for an explicit Lambda",
      "input": "X = [0 1; 1 1; 2 1; 3 1; 4 1];\ny = [1; 3; 5; 7; 9];\nB = lasso(X, y, \"Lambda\", 0, \"Standardize\", false)",
      "output": "B is approximately [2; 0]. Use FitInfo.Intercept for the constant term."
    },
    {
      "description": "Return FitInfo for an elastic-net path",
      "input": "[B,FitInfo] = lasso(X, y, \"Alpha\", 0.5, \"Lambda\", [0 0.01 0.1]);\ncoef0 = FitInfo.Intercept(1)",
      "output": "B has one column per Lambda value; FitInfo contains Lambda, Alpha, Intercept, DF, and MSE."
    },
    {
      "description": "Use K-fold cross-validation",
      "input": "[B,FitInfo] = lasso(X, y, \"CV\", 5);\nidx = FitInfo.IndexMinMSE;\ncoef = B(:,idx)",
      "output": "FitInfo includes cross-validation MSE, SE, and Lambda selection fields."
    },
    {
      "description": "Weight observations",
      "input": "w = ones(size(y));\nw(1:5) = 2;\n[B,FitInfo] = lasso(X, y, \"Weights\", w)",
      "output": "Rows with larger weights contribute more to the weighted least-squares objective."
    }
  ],
  "faqs": [
    {
      "question": "Where is the intercept stored?",
      "answer": "The coefficient matrix `B` contains only predictor coefficients. Intercepts are returned in `FitInfo.Intercept`, one value per Lambda."
    },
    {
      "question": "Does RunMat implement covariance-matrix acceleration?",
      "answer": "No. RunMat accepts `UseCovariance` for script compatibility, but fitting uses the direct coordinate-descent path and `FitInfo.UseCovariance` is false."
    },
    {
      "question": "How are Lambda values ordered?",
      "answer": "RunMat returns Lambda values in ascending order in `FitInfo.Lambda`, and the columns of `B` use the same order."
    },
    {
      "question": "Does cross-validation match MATLAB's random partitions exactly?",
      "answer": "No. RunMat currently uses deterministic round-robin K-fold partitions. The returned fields and selection semantics are MATLAB-compatible, but fold assignment is deterministic."
    }
  ],
  "links": [
    {
      "label": "corr",
      "url": "./corr"
    },
    {
      "label": "normalize",
      "url": "./normalize"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/stats/ml/lasso.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/stats/ml/lasso.rs"
  }
}
