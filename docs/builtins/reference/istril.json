{
  "title": "istril",
  "category": "math/linalg/structure",
  "keywords": [
    "istril",
    "lower triangular",
    "matrix structure",
    "sparse",
    "gpu"
  ],
  "summary": "Determine whether a matrix is lower triangular in MATLAB and RunMat.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [
      "f32",
      "f64"
    ],
    "broadcasting": "none",
    "notes": "Uses the provider `bandwidth` hook for real/logical gpuArray inputs and reads back only the small bandwidth result; complex/provider-missing paths fall back to the host."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 1,
    "constants": "inline"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::math::linalg::structure::diagonal_triangular::tests",
    "integration": "builtins::math::linalg::structure::diagonal_triangular::tests::gpu_inputs_use_provider_bandwidth_without_gathering"
  },
  "description": "`istril(A)` returns true when every element above the main diagonal of matrix `A` is zero.",
  "behaviors": [
    "Scalars are lower triangular.",
    "Diagonal and all-zero matrices are lower triangular.",
    "Rectangular matrices are supported.",
    "Dense real, logical, complex, sparse, and gpuArray inputs are supported.",
    "Complex entries count as nonzero when either real or imaginary part is nonzero.",
    "NaN values count as nonzero when testing matrix structure.",
    "Arrays with a non-singleton dimension beyond the second return `false`, matching MATLAB matrix-predicate behavior."
  ],
  "examples": [
    {
      "description": "Checking a lower-triangular matrix",
      "input": "A = [1 0 0; 2 3 0; 4 5 6];\ntf = istril(A)",
      "output": "tf = logical\n   1"
    },
    {
      "description": "Detecting a value above the main diagonal",
      "input": "B = [1 7; 0 2];\ntf = istril(B)",
      "output": "tf = logical\n   0"
    },
    {
      "description": "Checking a rectangular matrix",
      "input": "C = [1 0; 2 3; 4 5];\ntf = istril(C)",
      "output": "tf = logical\n   1"
    },
    {
      "description": "Inspecting a GPU-resident matrix",
      "input": "G = gpuArray(tril(ones(3)));\ntf = istril(G)",
      "output": "tf = logical\n   1"
    }
  ],
  "faqs": [
    {
      "question": "What entries does `istril` inspect?",
      "answer": "Only entries above the main diagonal can make the predicate false."
    },
    {
      "question": "Are diagonal matrices lower triangular?",
      "answer": "Yes. Diagonal matrices have zeros above the main diagonal, so `istril` returns true."
    },
    {
      "question": "How are sparse matrices handled?",
      "answer": "RunMat scans stored sparse entries without densifying and returns false only when a stored nonzero appears above the main diagonal."
    }
  ],
  "links": [
    {
      "label": "isdiag",
      "url": "./isdiag"
    },
    {
      "label": "istriu",
      "url": "./istriu"
    },
    {
      "label": "tril",
      "url": "./tril"
    },
    {
      "label": "bandwidth",
      "url": "./bandwidth"
    },
    {
      "label": "gpuArray",
      "url": "./gpuarray"
    },
    {
      "label": "gather",
      "url": "./gather"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/math/linalg/structure/diagonal_triangular.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/math/linalg/structure/diagonal_triangular.rs"
  },
  "gpu_residency": "The result is always a host logical scalar. For real/logical gpuArray inputs, RunMat keeps the matrix on the provider and uses the `bandwidth` hook to read back only lower/upper bandwidth metadata. Complex-interleaved GPU inputs and providers without `bandwidth` support fall back to the host path.",
  "gpu_behavior": [
    "`istril` is registered as a structure-analysis fusion sink. GPU-resident real/logical inputs use provider bandwidth (`upper == 0`) instead of gathering the full matrix when the active provider supports it."
  ]
}
