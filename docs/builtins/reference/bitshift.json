{
  "title": "bitshift",
  "category": "logical/bit",
  "keywords": ["bitshift", "bitwise", "shift", "integer"],
  "summary": "Shift integer-valued scalars and arrays left or right by bit counts.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [],
    "broadcasting": "scalar-or-exact-size",
    "notes": "Documented gpuArray calls require at least one integer-array input, unsigned non-64-bit A, no 64-bit integer input, and no assumedtype; exact fallback restores the result."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 2,
    "constants": "none"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::logical::bit::integer::tests",
    "integration": null
  },
  "description": "`bitshift(A, k)` shifts integer-valued input `A` left for positive `k` and right for negative `k`. `A` and `k` must be scalar or exactly the same size; `bitshift(A, k, assumedtype)` selects the signed or unsigned interpretation width for double `A`.",
  "behaviors": [
    "A accepts double and all eight built-in integer classes; k independently accepts double and all eight integer classes.",
    "Positive shifts move bits left and truncate overflow bits; negative shifts move bits right and preserve the sign bit for signed A.",
    "Shift magnitudes at least the width of A produce zero, except a negative shift of negative signed A produces all one-bits.",
    "Output preserves the data type of A; the class of k does not affect output class.",
    "Rejects fractional, infinite, NaN, complex, logical, single, string, cell, struct, and nonscalar unequal-size inputs in MATLAB-compatible mode."
  ],
  "examples": [
    {
      "description": "Left and right shifts",
      "input": "bitshift(uint32(3), 2)\nbitshift(uint32(8), -1)",
      "output": "ans = uint32\n  12\nans = uint32\n  4"
    }
  ],
  "faqs": [
    {
      "question": "Can shift counts be arrays?",
      "answer": "Yes. `A` and `k` can each be scalar, or they can be arrays with exactly the same size; general singleton expansion is not supported."
    }
  ],
  "links": [
    {
      "label": "bitand",
      "url": "./bitand"
    },
    {
      "label": "bitor",
      "url": "./bitor"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/logical/bit/integer.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/logical/bit/integer.rs"
  },
  "gpu_residency": "Documented GPU calls gather exactly for host execution and restore the output class of A to the owning provider.",
  "gpu_behavior": [
    "At least one input must be an integer array, A cannot be signed, 64-bit integers are unsupported in either input, and assumedtype is unsupported.",
    "RunMat mode separately gates single A, single k, logical A, logical k, broader resident input combinations, and resident assumedtype calls as extensions."
  ]
}
