{
  "title": "unicode2native",
  "category": "strings/core",
  "keywords": [
    "unicode2native",
    "encoding",
    "UTF-8",
    "latin1",
    "UTF-16",
    "UTF-32",
    "uint8",
    "text"
  ],
  "summary": "Convert Unicode text to encoded uint8 bytes in MATLAB and RunMat.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [],
    "broadcasting": "none",
    "notes": "Text encoding runs on the CPU. GPU-resident inputs are gathered before validation and encoding."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 2,
    "constants": "inline"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::strings::core::unicode2native::tests",
    "integration": "builtins::strings::core::unicode2native::tests::unicode2native_defaults_to_utf8_row_vector"
  },
  "description": "`unicode2native(unicodestr)` converts a string scalar or character vector into a `1xn uint8` row vector using UTF-8 by default. `unicode2native(unicodestr, encoding)` uses a named character encoding.",
  "behaviors": [
    "String scalars and character row vectors are accepted as input text.",
    "The optional encoding argument can be a string scalar or character row vector.",
    "The default encoding is UTF-8.",
    "Encoding names are resolved through the runtime encoding library plus explicit Unicode encoders, including common aliases such as `UTF-8`, `latin1`, `US-ASCII`, `windows-1252`, `Shift_JIS`, `UTF-16`, and `UTF-32`.",
    "`UTF-16` emits a little-endian byte-order mark followed by UTF-16LE bytes, matching MATLAB's documented output form.",
    "`UTF-16LE` and `UTF-16BE` produce UTF-16 bytes without a byte-order mark.",
    "`UTF-32` emits a little-endian byte-order mark followed by UTF-32LE bytes. `UTF-32LE` and `UTF-32BE` produce UTF-32 bytes without a byte-order mark.",
    "Characters that cannot be represented in a legacy byte encoding are substituted with `?` (`63`) instead of HTML numeric character references.",
    "The result is always a host `uint8` tensor with shape `1xn`, including `1x0` for empty input.",
    "Non-scalar string arrays, multi-row character arrays, unsupported encodings, non-text inputs, and extra arguments raise descriptive errors."
  ],
  "examples": [
    {
      "description": "Convert Text With The Default Encoding",
      "input": "bytes = unicode2native(\"hello\")",
      "output": "bytes =\n    1x5 uint8 row vector\n    104  101  108  108  111"
    },
    {
      "description": "Encode Text As UTF-8",
      "input": "bytes = unicode2native(\"hé\", \"UTF-8\")",
      "output": "bytes =\n    1x3 uint8 row vector\n    104  195  169"
    },
    {
      "description": "Encode Text As Latin-1",
      "input": "bytes = unicode2native('café', 'latin1')",
      "output": "bytes =\n    1x4 uint8 row vector\n    99  97  102  233"
    },
    {
      "description": "Encode Text As UTF-16",
      "input": "bytes = unicode2native(\"hi\", \"UTF-16\")",
      "output": "bytes =\n    1x6 uint8 row vector\n    255  254  104  0  105  0"
    },
    {
      "description": "Encode Text As UTF-32",
      "input": "bytes = unicode2native(\"A\", \"UTF-32\")",
      "output": "bytes =\n    1x8 uint8 row vector\n    255  254  0  0  65  0  0  0"
    }
  ],
  "faqs": [
    {
      "question": "What does `unicode2native` return?",
      "answer": "It returns a `uint8` row vector containing the encoded byte values."
    },
    {
      "question": "What is the default encoding?",
      "answer": "RunMat uses UTF-8 as the default native encoding."
    },
    {
      "question": "Can I pass a string array?",
      "answer": "Only scalar string arrays are accepted. MATLAB documents `unicodestr` as a string scalar or character vector, so nonscalar string arrays raise an error."
    },
    {
      "question": "What happens when a character cannot be represented?",
      "answer": "RunMat substitutes `?` (`63`) for characters that cannot be represented in byte-oriented legacy encodings. This avoids HTML numeric character-reference bytes and matches MATLAB-style byte substitution."
    },
    {
      "question": "Does this run on the GPU?",
      "answer": "No. This is a text conversion sink: inputs are gathered to host memory and encoded on the CPU."
    }
  ],
  "links": [
    {
      "label": "native2unicode",
      "url": "./native2unicode"
    },
    {
      "label": "string",
      "url": "./string"
    },
    {
      "label": "char",
      "url": "./char"
    },
    {
      "label": "uint8",
      "url": "./uint8"
    },
    {
      "label": "fwrite",
      "url": "./fwrite"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/strings/core/unicode2native.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/strings/core/unicode2native.rs"
  },
  "syntax": {
    "example": {
      "description": "Syntax",
      "input": "bytes = unicode2native(unicodestr)\nbytes = unicode2native(unicodestr, encoding)"
    },
    "points": [
      "`unicodestr` must be a string scalar or character vector.",
      "`encoding` must be a string scalar or character vector containing an encoding name.",
      "Returns `bytes`, a `uint8` row vector."
    ]
  }
}
