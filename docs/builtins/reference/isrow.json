{
  "title": "isrow",
  "category": "array/introspection",
  "keywords": ["isrow", "row vector", "shape", "metadata"],
  "summary": "Return true when an array has exactly one row.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [],
    "broadcasting": "none",
    "notes": "Reads shape metadata; gathers only when a GPU handle lacks shape metadata."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 0,
    "constants": "inline"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::array::introspection::isrow::tests",
    "integration": null
  },
  "description": "`isrow(A)` returns true when the first MATLAB-visible dimension of `A` is one.",
  "behaviors": [
    "Scalars and 1-by-N arrays return true.",
    "N-by-1 arrays with N other than one return false.",
    "The predicate uses the same dimension metadata as `size`."
  ],
  "examples": [
    {
      "description": "Testing row and column vectors",
      "input": "isrow([1 2 3])\nisrow([1; 2; 3])",
      "output": "ans = logical(1)\nans = logical(0)"
    }
  ],
  "faqs": [
    {
      "question": "Is a scalar a row?",
      "answer": "Yes. A scalar is 1-by-1."
    }
  ],
  "links": [
    { "label": "iscolumn", "url": "./iscolumn" },
    { "label": "isvector", "url": "./isvector" },
    { "label": "size", "url": "./size" }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/array/introspection/isrow.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/array/introspection/isrow.rs"
  },
  "gpu_residency": "The result is a host logical scalar.",
  "gpu_behavior": ["No GPU kernels are launched."]
}
