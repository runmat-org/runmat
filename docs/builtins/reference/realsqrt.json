{
  "title": "realsqrt",
  "category": "math/elementwise",
  "keywords": [
    "realsqrt",
    "square root",
    "real square root",
    "elementwise",
    "gpu"
  ],
  "summary": "Compute real square roots element-wise and reject values that require complex results.",
  "gpu_support": {
    "elementwise": true,
    "reduction": false,
    "precisions": [
      "f32",
      "f64"
    ],
    "broadcasting": "matlab",
    "notes": "RunMat uses provider reduce_min plus sqrt kernels only after proving gpuArray inputs are real and nonnegative; otherwise it gathers to enforce real-domain errors."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 1,
    "constants": "inline"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::math::elementwise::realsqrt::tests",
    "integration": "builtins::math::elementwise::realsqrt::tests::gpu_provider_roundtrip"
  },
  "description": "`Y = realsqrt(X)` computes `sqrt(X)` for real nonnegative inputs. Unlike `sqrt`, it rejects negative real values and complex inputs instead of producing complex results.",
  "behaviors": [
    "`realsqrt(X)` applies the operation element-wise and preserves the input shape.",
    "Logical values convert to doubles (`true -> 1.0`, `false -> 0.0`) before the square root is taken.",
    "Character arrays are interpreted as their numeric code points and return dense double tensors.",
    "Sparse inputs preserve sparsity: stored values are square-rooted and unstored zeros remain zero.",
    "Negative real values raise a domain error because the result would be complex.",
    "Complex scalar and complex tensor inputs are rejected, even if their imaginary parts are zero.",
    "NaNs and positive infinities propagate according to IEEE real square-root arithmetic."
  ],
  "examples": [
    {
      "description": "Taking the real square root of a scalar",
      "input": "y = realsqrt(9)",
      "output": "y = 3"
    },
    {
      "description": "Square roots of a matrix",
      "input": "A = [1 4 9; 16 25 36];\nR = realsqrt(A)",
      "output": "R = [1 2 3; 4 5 6]"
    },
    {
      "description": "Rejecting negative values",
      "input": "realsqrt([-1 4])",
      "output": "Error using realsqrt\nInput must be nonnegative."
    },
    {
      "description": "Preserving sparse storage",
      "input": "S = sparse([1 3], [1 2], [4 9], 3, 2);\nR = realsqrt(S)",
      "output": "R = sparse([1 3], [1 2], [2 3], 3, 2)"
    },
    {
      "description": "Running on GPU data",
      "input": "G = gpuArray([0 1; 4 9]);\nout = realsqrt(G);\nresult = gather(out)",
      "output": "result = [0 1; 2 3]"
    }
  ],
  "faqs": [
    {
      "question": "How is `realsqrt` different from `sqrt`?",
      "answer": "`sqrt` promotes negative real values to complex results. `realsqrt` stays in the real domain and raises an error when an element is negative."
    },
    {
      "question": "Does `realsqrt` accept complex inputs with zero imaginary part?",
      "answer": "No. Complex inputs are rejected because the function is a real-domain helper."
    },
    {
      "question": "Can `realsqrt` run on GPU data?",
      "answer": "Yes. RunMat first rejects complex GPU buffers, then proves that the GPU tensor has no negative values. If that proof succeeds and the provider exposes both reduce_min and sqrt hooks, the operation runs on the provider; otherwise RunMat gathers to host and validates the real-domain contract there."
    },
    {
      "question": "Does `realsqrt` fuse into larger GPU expressions?",
      "answer": "No. The builtin is treated as a fusion sink because negative inputs must raise a domain error rather than becoming NaN inside a fused shader."
    },
    {
      "question": "What happens to NaN values?",
      "answer": "`NaN` values remain `NaN`. They are not treated as negative, but they can force RunMat to validate GPU inputs on the host before execution."
    }
  ],
  "links": [
    {
      "label": "sqrt",
      "url": "./sqrt"
    },
    {
      "label": "power",
      "url": "./power"
    },
    {
      "label": "abs",
      "url": "./abs"
    },
    {
      "label": "real",
      "url": "./real"
    },
    {
      "label": "gpuArray",
      "url": "./gpuarray"
    },
    {
      "label": "gather",
      "url": "./gather"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/math/elementwise/realsqrt.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/math/elementwise/realsqrt.rs"
  },
  "gpu_residency": "RunMat keeps real gpuArray inputs on the active provider when it can prove the values are nonnegative and the provider exposes the required reduce_min and unary square-root hooks. Complex gpuArray inputs are rejected before provider execution. If the nonnegative proof is unavailable, if NaNs prevent a reduction proof, or if the provider lacks the hook, RunMat gathers to host and applies the same real-domain validation as CPU inputs.",
  "gpu_behavior": [
    "`realsqrt` is GPU-aware but not fused. Provider execution is used only after a nonnegative-domain check. This preserves MATLAB-compatible error behavior for negative inputs while still allowing nonnegative gpuArray inputs to remain device-resident."
  ]
}
