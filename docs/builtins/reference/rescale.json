{
  "title": "rescale",
  "category": "math/elementwise",
  "keywords": [
    "rescale",
    "normalize",
    "range",
    "InputMin",
    "InputMax",
    "gpu"
  ],
  "summary": "Scale the range of array elements in MATLAB and RunMat.",
  "gpu_support": {
    "elementwise": false,
    "reduction": true,
    "precisions": [
      "f32",
      "f64"
    ],
    "broadcasting": "matlab",
    "notes": "RunMat accepts gpuArray inputs by computing the documented semantics through a host round trip, then uploading the result back to the active provider so the output remains a gpuArray."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 5,
    "constants": "inline"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::math::elementwise::rescale::tests",
    "integration": null,
    "gpu": "builtins::math::elementwise::rescale::tests::gpu_input_returns_gpu_value_and_matches_cpu"
  },
  "description": "`R = rescale(A)` scales array values to `[0,1]` using the minimum and maximum over all elements of `A`. `rescale(A,l,u)` scales to `[l,u]`, and `InputMin`/`InputMax` name-value options override the input range and clip values before scaling.",
  "behaviors": [
    "The default range is computed from all elements of `A`, not independently by row or column.",
    "The output interval bounds `l` and `u`, and the `InputMin` and `InputMax` values, can be scalars or arrays that are implicitly expandable with `A`.",
    "Supplying vector `InputMin` and `InputMax` values supports independent row-wise or column-wise scaling while preserving MATLAB implicit expansion behavior.",
    "Values below `InputMin` are clipped to `InputMin`; values above `InputMax` are clipped to `InputMax` before scaling.",
    "Constant input ranges return the lower output bound, or `NaN` when the output interval contains `Inf`.",
    "Single-precision tensor inputs preserve single dtype metadata; other numeric and logical inputs return double values.",
    "NaN values are omitted when computing the default input range, and NaN input elements remain NaN in the corresponding output positions. All-NaN inputs produce NaN outputs.",
    "Complex, string, cell, struct, object, and function-handle inputs are rejected."
  ],
  "examples": [
    {
      "description": "Scale a vector to the unit interval",
      "input": "A = 1:5;\nR = rescale(A)",
      "output": "R = [0 0.25 0.5 0.75 1]"
    },
    {
      "description": "Scale a vector to a custom interval",
      "input": "A = 1:5;\nR = rescale(A,-1,1)",
      "output": "R = [-1 -0.5 0 0.5 1]"
    },
    {
      "description": "Clip to an input range before scaling",
      "input": "A = [-30 1 2 3 4 5 70];\nR = rescale(A,\"InputMin\",1,\"InputMax\",5)",
      "output": "R = [0 0 0.25 0.5 0.75 1 1]"
    },
    {
      "description": "Scale matrix columns independently",
      "input": "A = [0.4 -4; 0.5 -5; 0.9 9; 0.2 1];\nR = rescale(A,\"InputMin\",min(A),\"InputMax\",max(A))",
      "output": "R = [0.2857 0.0714; 0.4286 0; 1 1; 0 0.4286]"
    }
  ],
  "faqs": [
    {
      "question": "Does `rescale(A)` operate column-wise?",
      "answer": "No. The default range uses `min(A(:))` and `max(A(:))` across all elements. Use vector `InputMin` and `InputMax` values, such as `min(A)` and `max(A)`, for independent column scaling."
    },
    {
      "question": "What happens when all input values are equal?",
      "answer": "RunMat returns the lower output bound, matching MATLAB. If the requested output interval contains `Inf`, the result is `NaN`."
    },
    {
      "question": "Can `InputMin` and `InputMax` be arrays?",
      "answer": "Yes. They may be scalars, vectors, matrices, or N-D arrays that are compatible with `A` under MATLAB implicit expansion rules."
    },
    {
      "question": "Does RunMat keep `rescale(gpuArray(...))` on the GPU?",
      "answer": "Yes. The current implementation may use a host round trip internally for documented argument forms, but it uploads the result back to the active provider when any operand is a gpuArray."
    }
  ],
  "links": [
    {
      "label": "bounds",
      "url": "./bounds"
    },
    {
      "label": "min",
      "url": "./min"
    },
    {
      "label": "max",
      "url": "./max"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/math/elementwise/rescale.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/math/elementwise/rescale.rs"
  }
}
