{
  "title": "ismembertol",
  "category": "array/sorting_sets",
  "keywords": [
    "ismembertol",
    "membership",
    "tolerance",
    "set",
    "rows",
    "indices",
    "datascale"
  ],
  "summary": "Test numeric membership within tolerance in MATLAB and RunMat.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [],
    "broadcasting": "none",
    "notes": "Documented GPU inputs use authoritative typed gather fallback and ordinary logical/index outputs are restored to the owning provider."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 2,
    "constants": "inline"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::array::sorting_sets::ismembertol::tests",
    "integration": "builtins::array::sorting_sets::ismembertol::tests::documented_resident_integer_outputs_restore_and_excess_arity_rejects"
  },
  "description": "`ismembertol(A, B)` returns logical membership flags for real numeric values in `A` that are within tolerance of values in `B`. Optional outputs return first or all matching one-based indices into `B`.",
  "behaviors": [
    "Without an explicit tolerance, RunMat uses MATLAB-compatible defaults: `1e-12` for double inputs and `1e-6` when either input is single precision.",
    "Element comparisons use `abs(A-B) <= tol * max(abs([A(:); B(:)]))` unless `DataScale` is supplied.",
    "`DataScale`, when scalar, changes the tolerance test to `abs(A-B) <= tol * DataScale`.",
    "`ByRows`, true compares complete rows and returns outputs of size `size(A,1) x 1`; A and B must have the same number of columns.",
    "In row mode, vector `DataScale` supplies one scale per column. A scale of `Inf` ignores that column during matching.",
    "The optional second output `LocB` contains the first one-based index in `B` for each match and `0` when absent.",
    "`OutputAllIndices`, true makes `LocB` a cell array whose cells contain all matching one-based indices, or `[0]` when no match exists.",
    "`NaN` values do not match. Exact equalities, including equal infinities, match before tolerance subtraction is considered.",
    "Complex, sparse, string, character, cell, struct, and object inputs are rejected. The documented host data and tolerance domains are `single` and `double`; RunMat mode additionally accepts host integer/logical data and typed integer/logical tolerance controls through declared compatibility extensions."
  ],
  "examples": [
    {
      "description": "Testing approximate membership with default scaling",
      "input": "A = [0.1 1e10];\nB = [0.1 + 1e-14, 1e10 + 5e-3];\nLIA = ismembertol(A, B)",
      "output": "LIA =\n  1x2 logical array\n   1   1"
    },
    {
      "description": "Returning first matching indices",
      "input": "A = [1 2 4];\nB = [2.01 2.02 4.2];\n[LIA, LocB] = ismembertol(A, B, 0.05)",
      "output": "LIA =\n  1x3 logical array\n   0   1   1\nLocB =\n     0     1     3"
    },
    {
      "description": "Using absolute tolerance with DataScale",
      "input": "A = 1e10;\nB = 1e10 + 5e-3;\nLIA = ismembertol(A, B, 1e-6, 'DataScale', 1)",
      "output": "LIA =\n     0"
    },
    {
      "description": "Comparing rows and ignoring one column",
      "input": "A = [0 0.5; 10 20];\nB = [0.05 999; 10.2 -999];\n[LIA, LocB] = ismembertol(A, B, 0.1, 'ByRows', true, 'DataScale', [1 Inf])",
      "output": "LIA =\n  2x1 logical array\n   1\n   0\nLocB =\n     1\n     0"
    },
    {
      "description": "Returning all matching locations",
      "input": "A = 2;\nB = [1.99 2.01 9.5];\n[LIA, LocB] = ismembertol(A, B, 0.01, 'OutputAllIndices', true)",
      "output": "LIA =\n     1\nLocB =\n  1x1 cell array\n    {[1; 2]}"
    },
    {
      "description": "Using gpuArray inputs",
      "input": "G = gpuArray([1; 2]);\nH = gpuArray([1 + 1e-13; 3]);\nLIA = ismembertol(G, H)",
      "output": "LIA =\n  2x1 logical array\n   1\n   0"
    }
  ],
  "faqs": [
    {
      "question": "How is the default tolerance chosen?",
      "answer": "Double inputs use `1e-12`. If either input is single precision, RunMat uses `1e-6`."
    },
    {
      "question": "Does `ismembertol` support string or cell inputs like `ismember`?",
      "answer": "No. MATLAB's tolerance membership operation is numeric; RunMat accepts real full numeric and logical inputs only."
    },
    {
      "question": "What does `OutputAllIndices` change?",
      "answer": "The second output becomes a cell array. Each cell contains every one-based index in `B` within tolerance of the corresponding element or row of `A`, or `[0]` when no match exists."
    },
    {
      "question": "Can `DataScale` be a vector?",
      "answer": "Only when `ByRows` is true. Each vector element scales the tolerance for the corresponding column; `Inf` ignores that column."
    },
    {
      "question": "Do NaN values match each other?",
      "answer": "No. `NaN` does not match any value, including another `NaN`, for `ismembertol`."
    },
    {
      "question": "Will `ismembertol` run on the GPU automatically?",
      "answer": "Documented gpuArray input includes integer classes through 32 bits but excludes 64-bit integers, `ByRows`, and `OutputAllIndices`. RunMat preserves those additional forms behind explicit compatibility gates."
    }
  ],
  "links": [
    {
      "label": "ismember",
      "url": "./ismember"
    },
    {
      "label": "unique",
      "url": "./unique"
    },
    {
      "label": "intersect",
      "url": "./intersect"
    },
    {
      "label": "setdiff",
      "url": "./setdiff"
    },
    {
      "label": "union",
      "url": "./union"
    },
    {
      "label": "issorted",
      "url": "./issorted"
    },
    {
      "label": "gpuArray",
      "url": "./gpuarray"
    },
    {
      "label": "gather",
      "url": "./gather"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/array/sorting_sets/ismembertol.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/array/sorting_sets/ismembertol.rs"
  },
  "gpu_behavior": [
    "`ismembertol` is registered as a sink builtin. When either input tensor lives on the GPU the runtime gathers authoritative typed storage to host memory and performs tolerance matching there.",
    "Documented ordinary logical and double-index outputs are restored to the first resident input handle's owning provider. Integer gpuArray inputs through 32 bits retain their documented support across the fallback.",
    "Resident 64-bit integer input and enabled `ByRows` or `OutputAllIndices` are RunMat-only extensions checked before gather. The extended all-indices cell result remains host materialized because the public GPU form excludes that output."
  ]
}
