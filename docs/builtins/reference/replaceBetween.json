{
  "title": "replaceBetween",
  "category": "strings/transform",
  "keywords": [
    "replaceBetween",
    "strings",
    "transform",
    "matlab",
    "compatibility"
  ],
  "summary": "Replace text between positions or boundary markers.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [],
    "broadcasting": "none",
    "notes": "No GPU kernel is registered. Automatic residency for numeric positions gathers transparently; explicit gpuArray host fallback is a declared RunMat extension."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": null,
    "constants": "not applicable",
    "notes": "Not eligible for fusion; this builtin materializes host text or logical results."
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::strings::transform::compat::tests",
    "integration": "crates/runmat-vm/tests/integer_label_and_text_control_semantics.rs"
  },
  "description": "s = replaceBetween(text, start, end, newText) - Replace text between positions or boundary markers.",
  "signatures": [
    {
      "label": "s = replaceBetween(text, start, end, newText)",
      "inputs": [
        {
          "name": "text",
          "ty": "Any",
          "arity": "Required",
          "default": null,
          "description": "Input text."
        },
        {
          "name": "arg",
          "ty": "Any",
          "arity": "Variadic",
          "default": null,
          "description": "Additional arguments."
        }
      ],
      "outputs": [
        {
          "name": "out",
          "ty": "Any",
          "arity": "Required",
          "default": null,
          "description": "Output value."
        }
      ]
    }
  ],
  "behaviors": [
    "Replace text between positions or boundary markers.",
    "Numeric boundaries are one-based inclusive character positions and accept every integer class through exact native validation.",
    "Text, start, end, and replacement inputs may each be scalar or have the same size; scalar values expand across the common text shape.",
    "Integer source or replacement values reject without implicit decimal formatting or provider access.",
    "Text and pattern boundaries replace the text between the matched delimiters and leave the delimiters in place."
  ],
  "examples": [
    "replaceBetween(\"a[old]b\", \"[\", \"]\", \"new\") returns \"a[new]b\"."
  ],
  "faqs": [],
  "links": [
    {
      "label": "string",
      "url": "./string"
    },
    {
      "label": "char",
      "url": "./char"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/strings/transform/compat.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/strings/transform/compat.rs"
  }
}
