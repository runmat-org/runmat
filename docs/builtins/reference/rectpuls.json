{
  "title": "rectpuls",
  "category": "math/signal",
  "keywords": [
    "rectpuls",
    "rectangular pulse",
    "pulse train",
    "signal processing",
    "elementwise"
  ],
  "summary": "Generate sampled rectangular pulses.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [
      "f64"
    ],
    "broadcasting": "none",
    "notes": "Automatic resident inputs may gather transparently for host evaluation and return through their exact owner. Explicit gpuArray inputs are a separately gated RunMat extension; admitted outputs retain explicit residency or fail if the owner cannot preserve double output."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 1,
    "constants": "inline",
    "notes": "Fusion is disabled because width validation and left-closed/right-open boundary semantics are handled by the host builtin."
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::math::signal::rectpuls::tests",
    "integration": null
  },
  "description": "`rectpuls(T)` returns samples of a unit-width rectangular pulse centered at zero. `rectpuls(T, W)` uses pulse width `W`.",
  "behaviors": [
    "Operates element-wise on real numeric scalars, vectors, matrices, and N-D tensors; preserves input shape.",
    "Returns `1` on the left-closed, right-open interval `-W/2 <= T < W/2` and `0` outside it, so `rectpuls(-0.5)` is `1` while `rectpuls(0.5)` is `0` for the default width.",
    "The width must be positive and finite.",
    "The documented sample-time classes are single and double. Typed-integer sample times and widths are independent RunMat-mode extensions, admitted only when their authoritative values are exactly representable at the binary64 waveform boundary.",
    "Complex and text inputs are rejected with a builtin-scoped error."
  ],
  "examples": [
    {
      "description": "Default unit-width rectangular pulse",
      "input": "t = -0.5:0.25:0.5;\ny = rectpuls(t)",
      "output": "y = [1 1 1 1 0]"
    },
    {
      "description": "Custom pulse width",
      "input": "t = -1:0.5:1;\ny = rectpuls(t, 0.5)",
      "output": "y = [0 0 1 0 0]"
    }
  ],
  "faqs": [
    {
      "question": "What happens at the exact pulse edge?",
      "answer": "The interval is left-closed and right-open: RunMat returns `1` at `T == -W/2` and `0` at `T == W/2`."
    },
    {
      "question": "Can `rectpuls` be used inside `pulstran`?",
      "answer": "Yes. `pulstran(T, D, 'rectpuls', W)` evaluates rectangular pulses centered at each delay."
    }
  ],
  "links": [
    {
      "label": "pulstran",
      "url": "./pulstran"
    },
    {
      "label": "tripuls",
      "url": "./tripuls"
    },
    {
      "label": "gauspuls",
      "url": "./gauspuls"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/math/signal/rectpuls.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/math/signal/rectpuls.rs"
  }
}
