{
  "title": "strtok",
  "category": "strings/core",
  "keywords": [
    "strtok",
    "strings",
    "core",
    "matlab",
    "compatibility"
  ],
  "summary": "Split text into first token and remainder.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [],
    "broadcasting": "none",
    "notes": "Tokenization is host text processing. Numeric and provider-resident text or delimiter inputs reject before provider access."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": null,
    "constants": "not applicable",
    "notes": "Not eligible for fusion; this builtin materializes host text or logical results."
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::strings::core::compat::tests",
    "integration": null
  },
  "description": "`[tok, rem] = strtok(text, delimiters)` skips leading delimiter characters, returns the first token, and optionally returns the remaining text beginning at the delimiter after that token.",
  "signatures": [
    {
      "label": "[tok, rem] = strtok(text, delimiters)",
      "inputs": [
        {
          "name": "text",
          "ty": "Any",
          "arity": "Required",
          "default": null,
          "description": "Input text."
        },
        {
          "name": "arg",
          "ty": "Any",
          "arity": "Variadic",
          "default": null,
          "description": "Additional arguments."
        }
      ],
      "outputs": [
        {
          "name": "out",
          "ty": "Any",
          "arity": "Required",
          "default": null,
          "description": "Output value."
        }
      ]
    }
  ],
  "behaviors": [
    "Split text into first token and remainder.",
    "When multiple outputs are requested, returns both the first token and the remaining text.",
    "Leading delimiters are skipped before token selection; the returned remainder begins at the delimiter after the token.",
    "Integer arrays are not character-code input and are rejected without provider access or implicit text conversion."
  ],
  "examples": [
    "[tok,rem] = strtok(\"  alpha,beta\", \" ,\") returns tok = \"alpha\" and rem = \",beta\"."
  ],
  "faqs": [],
  "links": [
    {
      "label": "string",
      "url": "./string"
    },
    {
      "label": "char",
      "url": "./char"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/strings/core/compat.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/strings/core/compat.rs"
  }
}
