{
  "title": "statget",
  "category": "stats/options",
  "keywords": [
    "statget",
    "statset",
    "statistics options"
  ],
  "summary": "Read values from statistics options structures.",
  "gpu_support": {
    "elementwise": false,
    "reduction": false,
    "precisions": [],
    "broadcasting": "none",
    "notes": "Options are host metadata. An automatically resident default value is gathered through its owning provider only when the selected field is empty and the default is needed."
  },
  "fusion": {
    "elementwise": false,
    "reduction": false,
    "max_inputs": 0,
    "constants": "inline"
  },
  "requires_feature": null,
  "tested": {
    "unit": "builtins::stats::options::tests",
    "integration": "crates/runmat-vm/tests/stats_ml.rs"
  },
  "description": "`statget(options,field)` reads one field from a statistics options structure, using case-insensitive unique-prefix matching. Add `defaultData` to supply a value when the recognized field is empty. The default is an opaque value rather than a numeric conversion: any integer class, shape, and exact payload is returned unchanged when selected.",
  "behaviors": [
    "`options` must be a struct, typically created by `statset`.",
    "`field` is matched case-insensitively and can be a unique leading prefix of a known statistics option field.",
    "If the requested field is recognized but empty, `statget` returns the supplied default value when present, otherwise it returns the empty value.",
    "If the requested field is not recognized or is ambiguous, RunMat returns an empty numeric value for compatibility with loose MATLAB option probing.",
    "`defaultData` accepts any data type. Native integer defaults keep their class, shape, and exact values without a floating compatibility mirror."
  ],
  "examples": [
    {
      "description": "Read an explicit option",
      "input": "opts = statset('TolX', 1e-8);\ntol = statget(opts, 'TolX')",
      "output": "`tol` is `1e-8`."
    },
    {
      "description": "Use a default for an empty option",
      "input": "opts = statset('MaxIter', 100);\ntol = statget(opts, 'TolFun', 1e-6)",
      "output": "`tol` is `1e-6` because `TolFun` is empty."
    },
    {
      "description": "Use a unique prefix",
      "input": "opts = statset('MaxIter', 100);\niter = statget(opts, 'MaxI')",
      "output": "`iter` is `100`."
    }
  ],
  "links": [
    {
      "label": "statset",
      "url": "./statset"
    }
  ],
  "source": {
    "label": "`crates/runmat-runtime/src/builtins/stats/options.rs`",
    "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/stats/options.rs"
  }
}
