{
  "title": "fea.model",
  "category": "fea",
  "keywords": ["fea", "model", "geometry", "components"],
  "summary": "Assemble a typed RunMat FEA model.",
  "requires_feature": "fea",
  "tested": { "unit": "builtins::fea::tests::typed_constructors", "integration": null },
  "description": "`fea.model(id, geometry, Name, Value, ...)` assembles an analysis model from a `geometry.Asset` and typed RunMat FEA components.",
  "behaviors": [
    "Model id and component identities remain exact structural values; the geometry revision is copied exactly into the model.",
    "Profile, frame, defaults mode, materials, assignments, boundaries, loads, steps, domains, and interfaces are validated during assembly.",
    "Numeric physics values must already have crossed their owning constructor's explicit binary64 boundary; `fea.model` does not reinterpret integer arrays as components.",
    "Model assembly consumes an existing geometry asset and does not intrinsically require native OCCT. OCCT is only relevant when a topology-bearing CAD file must first be imported."
  ],
  "examples": [{ "description": "Assemble an explicit model", "input": "model = fea.model(\"bracket\", geom, \"Materials\", {steel}, \"Loads\", {tipLoad});" }],
  "faqs": [{ "question": "Does `fea.model` import CAD?", "answer": "No. Pass a `geometry.Asset`. CAD import is a separate geometry operation and only some CAD formats require an OCCT-enabled importer." }],
  "links": [{ "label": "FEA models", "url": "../../fea/models" }],
  "source": { "label": "`crates/runmat-runtime/src/builtins/fea/mod.rs`", "url": "https://github.com/runmat-org/runmat/blob/main/crates/runmat-runtime/src/builtins/fea/mod.rs" }
}
