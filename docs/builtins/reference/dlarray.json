{
  "title": "dlarray",
  "category": "deep_learning",
  "keywords": ["dlarray", "deep learning", "array labels"],
  "summary": "Create a dlarray compatibility object around host or gpuArray data.",
  "description": "`dlarray(X)` wraps host or provider-resident gpuArray data and optional dimension labels in a RunMat compatibility object.",
  "behaviors": [
    "The underlying `Data` value is preserved without conversion, including provider-resident `gpuArray` handles.",
    "A second text argument is stored as `Format` and `Labels`.",
    "Additional constructor options are rejected with explicit compatibility errors.",
    "Inside `dlfeval`, host `dlarray` values become traced leaves for the bounded `dlgradient` tape. Supported arithmetic preserves the wrapper and dimension-label metadata."
  ],
  "examples": [
    {
      "description": "Wrap Data",
      "input": "X = dlarray(rand(3,4), 'CB')",
      "output": "dlarray compatibility object."
    }
  ],
  "syntax": {
    "example": {
      "description": "Supported forms",
      "input": "X = dlarray(data)\nX = dlarray(data, fmt)",
      "output": "dlarray compatibility object."
    }
  },
  "gpu_residency": "`dlarray` preserves provider-resident `gpuArray` data as wrapper metadata outside autodiff. GPU-backed `dlarray` tracing is rejected with an explicit provider-gap error until Deep Learning tape kernels are available in the acceleration provider."
}
